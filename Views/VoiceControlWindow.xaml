<Window x:Class="PromptMasterv5.Views.VoiceControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PromptMasterv5.Views"
        xmlns:vm="clr-namespace:PromptMasterv5.ViewModels"
        mc:Ignorable="d"
        Title="Voice Control" Height="100" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Border Background="#CC000000" CornerRadius="12" Padding="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <TextBlock Text="🎙️" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusText}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            
            <TextBlock Text="{Binding RecognizedText}" 
                       FontSize="14" 
                       Foreground="#CCCCCC" 
                       HorizontalAlignment="Center" 
                       TextTrimming="CharacterEllipsis"/>
            
            <!-- Simple Visualizer (Width based on AudioLevel) -->
            <Rectangle Height="4" Fill="#4CAF50" HorizontalAlignment="Center" Margin="0,8,0,0" RadiusX="2" RadiusY="2">
                <Rectangle.Width>
                    <Binding Path="AudioLevel" Mode="OneWay"/>
                </Rectangle.Width>
            </Rectangle>

        </StackPanel>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
